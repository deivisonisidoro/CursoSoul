{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
    {{else}}
{{/each}}
<form action="./novorelatorio" method="POST">
    <div class="card">
        <h2>{{aluno.nome}}</h2>
        <div class="card-body">
            <h4>Relátorio:</h4>
                
            <textarea name="relatorio" id="relatorio" cols="30" rows="10" class="form-control"></textarea>
            
            <button type="submit" class="btn btn-dark mt-2">Enviar relatorio</button>   
        </div>
        
    </div>
</form>