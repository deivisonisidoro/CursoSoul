{{#each erro}}
    <div class="alert alert-danger">{{texto}}</div>
    {{else}}
{{/each}}
<h3>Novo aluno</h3>
<div class="card">
    <div class="card-body">
        <form action="/admin/add" method="POST">
            <label for="email">Email:</label>
                <input type="email" name="email" id="email" placeholder="email do aluno" class="form-control">
        
            <label for="senha">Senha:</label>
                    <input type="password" name="senha" id="senha" placeholder="Criando senha" class="form-control">
            
            <label for="senha2Admin">Repita a sua senha:</label>
                    <input type="password" name="senha2" id="senha2" placeholder="Digite a senha novamente" class="form-control" >
            <br>
            <button type="submit" class="btn btn-dark">Criar aluno</button>
        </form>
    </div>
</div>